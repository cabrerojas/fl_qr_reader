import 'package:flutter/material.dart';

class AdressesPage extends StatelessWidget {
  const AdressesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('AdressesPage'),
    );
  }
}
